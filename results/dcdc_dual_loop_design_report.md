# DC/DC Buck 开关电路与电压电流双闭环 PI 控制设计报告

生成日期：2026-05-10 00:33:40

## 1. 设计目标与假设

- 拓扑：降压型 Buck DC/DC，PWM 开关级模型。
- 输入电压：48.0 V，输出目标：24.0 V，额定功率：120.0 W，等效负载：4.80 ohm。
- 开关频率：50 kHz，仿真步长：1.0 us。
- 功率级参数：L = 220 uH，C = 470 uF，二极管压降按 0.7 V 计。
- 扰动工况：22.0 ms 负载加重到 50%，30.0 ms 输入电压降到 90%。

## 2. 功率级与控制结构

模型文件为 `dcdc_buck_dual_loop_switching.slx`。模型包含 `Dual_Loop_PI_PWM` 和 `Buck_Switching_Plant` 两个核心子模块：前者实现电压外环、电流内环、限幅抗积分饱和与 PWM 载波比较；后者实现 Buck 开关导通/续流两种状态下的电感电流和输出电容电压更新。

控制链路为：`Vref -> 电压 PI -> iL_ref -> 电流 PI -> duty -> PWM -> Buck 功率级 -> Vo/iL`。电压环输出限幅为 0.0 到 12.0 A，占空比限幅为 0.02 到 0.95。

## 3. PI 参数仿真整定过程

按照仓库技能中的顺序，先固定电压外环，只调电流内环；随后固定内环，再搜索电压外环。每组参数均计算超调、调节时间、稳态误差、尾段振荡、电流峰值和占空比限幅比例。

电流内环候选最优：`Kp_i = 0.07`，`Ki_i = 900`，内环评分 0.1434。

电压外环候选最优：`Kp_v = 0.2`，`Ki_v = 110`，外环评分 0.2608。

## 4. 当前已验证候选参数

| 参数 | 数值 | 状态 |
| --- | ---: | --- |
| Kp_i | 0.07 | 在该模型下已验证 |
| Ki_i | 900 | 在该模型下已验证 |
| Kp_v | 0.2 | 在该模型下已验证 |
| Ki_v | 110 | 在该模型下已验证 |

## 5. 开关级验证指标

| 指标 | 结果 |
| --- | ---: |
| 输出电压超调 | 0.68 % |
| 调节时间 | 12.67 ms |
| 稳态误差 | 1.93 % |
| 尾段电压标准差 | 0.145 V |
| 电感电流峰值 | 10.522 A |
| 稳态电感电流纹波 | 1.357 A |
| 占空比限幅比例 | 3.16 % |

## 6. 结论与使用说明

本轮结果属于“在该模型和上述工况下已验证”的候选参数。模型中包含软启动、负载阶跃、输入电压阶跃、占空比限幅和电流参考限幅，因此参数不是静态公式猜测，而是通过仿真指标搜索得到。

后续若要面向真实硬件，还需要加入 MOSFET 导通电阻、二极管反向恢复、ESR/ESL、采样延迟、ADC 量化、PWM 死区、驱动延迟和保护逻辑，再在输入电压/负载全范围内做边界验证。

波形图：`results/dcdc_dual_loop_waveforms.png`。仿真数据：`results/dcdc_dual_loop_results.mat`。

生成的 Simulink 模型已通过 `SimulationInput` 执行一次，停止事件：`ReachedStopTime`。
